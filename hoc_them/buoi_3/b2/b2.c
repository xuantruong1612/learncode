#include <stdio.h>

int main() {
    int a;
    scanf("%d", &a);
    if (a < 0) {
    }
    else {
        if (a >= 18)
            printf("em da truong thanh");
        else
            printf("em chua 18");
    }
    return 0;
}
